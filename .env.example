# NPM MCP Generator Environment Variables
# 
# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual API keys below
# 3. The .env file is automatically loaded when you run the generator

# OpenAI API Key (required for vector embeddings)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# GitHub API Token (optional, for better rate limits)
# Get a personal access token from: https://github.com/settings/tokens
GITHUB_TOKEN=ghp_your-github-token-here

# Default configuration options
# Uncomment and modify as needed

# Default output directory for generated MCP servers
DEFAULT_OUTPUT_DIR=./generated-servers

# Default maximum pages to crawl for documentation
DEFAULT_MAX_PAGES=5

# Default maximum chunks for vector embeddings (higher = more comprehensive but slower/costlier)
DEFAULT_MAX_CHUNKS=1000

# Enable verbose logging by default
# DEFAULT_VERBOSE=true

# Enable embeddings by default (requires OPENAI_API_KEY)
DEFAULT_EMBEDDINGS=true